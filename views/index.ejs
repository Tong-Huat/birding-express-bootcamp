<%- include('header', {title: `Birding Record`}); -%>

  <h1>Notes Record</h1>
  <table class="table table-striped table-sm table-responsive-md">
    <thead>
      <tr class="thead">
        <th scope="col">Report</th>
        <th scope="col">Date</th>
        <th scope="col">Summary</th>
        <th scope="col" colspan="2">Actions</th>
      </tr>
    </thead>
    <tbody>
      <!-- Create an row for each sighting -->
      <% for (let i=0; i < data.length; i +=1) {%>
        <tr>
          <th scope="row" class="theader">
            <%= i +1 %>
          </th>
          <td class="tdata">
            <a href="<%= `/note/${i + 1}` %>">
              <%= data[i].date %>
            </a>
          </td>
          <td class="tdata">
            <%= data[i].behaviour %>
          </td>
          <td>
            <form action="/note/<%= i %>?_method=DELETE" method="POST">
              <input disabled type="submit" value="Delete" />
            </form>
          </td>
          <td><button disabled><a href="#"> Edit </a></button>
          </td>
        </tr>
        <% }; %>
    </tbody>
  </table>


  <%- include('footer'); -%>